# PostgreSQL — ne jamais committer le fichier .env réel
#
# Local (Docker dev) :
#   docker compose up -d
#   DATABASE_URL="postgresql://tamrix:tamrix_dev_change_me@localhost:5432/tamrix?schema=public"
#
# Production Infomaniak (docker-compose.prod.yml) :
#   POSTGRES_PASSWORD=...  (obligatoire)
#   DATABASE_URL est injecté automatiquement vers le conteneur db
#
# Production Neon (optionnel, sans Postgres sur le VPS) :
#   DATABASE_URL = connection string Neon avec ?sslmode=require

DATABASE_URL="postgresql://tamrix:tamrix_dev_change_me@localhost:5432/tamrix?schema=public"

# Production Infomaniak — mot de passe du conteneur PostgreSQL
POSTGRES_PASSWORD="changez-moi-postgres-tres-long"

# Panneau admin — premier compte (créé automatiquement si la table est vide)
# Exemple : openssl rand -base64 32
ADMIN_EMAIL="admin@tmrix.com"
ADMIN_PASSWORD="changez-moi-mot-de-passe-tres-long"
ADMIN_SECRET="changez-moi-secret-hmac-tres-long"

# Notifications e-mail (Resend — https://resend.com)
# Admin : alerte à chaque devis / commande
# Client : e-mail de confirmation automatique (même config)
ADMIN_NOTIFICATION_EMAIL="vous@example.com"
RESEND_API_KEY="re_xxxxxxxx"
EMAIL_FROM="Tamrix <notifications@tmrix.com>"
# URL publique du site (HTTPS obligatoire en production)
# Utilisez le domaine principal choisi dans Vercel (tmrix.com ou www.tmrix.com)
APP_URL="https://www.tmrix.com"

# Rate limiting production (requis sur Vercel — sans Upstash, les formulaires publics sont bloqués)
# Créez une base Redis gratuite sur https://upstash.com
UPSTASH_REDIS_REST_URL="https://..."
UPSTASH_REDIS_REST_TOKEN="..."

# Cloudflare Turnstile (captcha) — https://dash.cloudflare.com → Turnstile
NEXT_PUBLIC_TURNSTILE_SITE_KEY="0x..."
TURNSTILE_SECRET_KEY="0x..."
